export type DataStyles = {
  icon: string[];
  dataContainer: string[];
  dataInfoContainer?: string[];
  sectionDataContainer?: string[];
}

export type DataParams = {
  showSteps?: boolean,
  styles: DataStyles,
}